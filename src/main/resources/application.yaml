server:
  port: 8080
  servlet:
    context-path: /identity


spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/identify_sesvice_db"
    username: user
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true